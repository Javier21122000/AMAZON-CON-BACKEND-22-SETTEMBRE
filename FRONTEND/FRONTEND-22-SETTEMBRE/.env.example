# The local Spring Boot server has no CORS policy; use Vite's /api proxy in development.
VITE_API_BASE_URL=/api
# Optional convenience presets. Never use production credentials in VITE_* variables.
VITE_TEST_ADMIN_EMAIL=javier@archivio00.it
VITE_TEST_ADMIN_PASSWORD=admin123
VITE_TEST_USER_EMAIL=utente@archivio00.it
VITE_TEST_USER_PASSWORD=password123
