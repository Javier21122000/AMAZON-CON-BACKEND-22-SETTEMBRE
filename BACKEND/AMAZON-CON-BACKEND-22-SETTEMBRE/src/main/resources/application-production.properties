server.port=${PORT:10000}
spring.datasource.url=${JDBC_DATABASE_URL}
app.jwt.secret=${JWT_SECRET}
app.seed-demo-users=false
app.cors.allowed-origin=${FRONTEND_ORIGIN:https://archivio00-frontend.onrender.com}
